# 设计简报 (Design Brief) — D1 产物模板

> 用途：把种子串灵感与用户口味收敛为**方向明确、可执行**的设计简报。每份简报对应一个候选方向；D1 出口至少 2 份，与用户对齐后选定 1 份深化。

## 1. 基础信息
- **产品/项目**: `<名称与一句话定位>`
- **目标用户**: `<谁在用，什么场景>`
- **期望情感反应**: `<打开页面的第一感受，例如：沉静、兴奋、被逗乐、信赖>`

## 2. 方向定义 (Direction)
- **灵感来源（种子串或其子模式）**: `<从种子串提取到的暗示，例如：连续块状分布 → 板块化排版; 数字 3/7 出现频率 → 三比七的版面分割>`
- **跨界灵感（可选）**: `<用户注入的灵感，例如：工业控制面板 / 等距 3D 城市 / 档案室>`

## 3. 四个决策域 (Four Decision Domains)
- **配色**: `<主色/辅色/中性色与理由>`
- **布局**: `<骨架与节奏，例如：强不对称 / 杂志式分栏 / 满幅沉浸式>`
- **字体**: `<字体栈与字号节奏；标题与正文的对比策略>`
- **质感**: `<材质与细节，例如：噪点纹理 / 玻璃折射 / 硬朗直角>`

## 4. 质量锚点与参考基线 (Moodboard)
- **参考作品**（锚点，禁止拷贝）: `<2~4 个方向参考——站内引用或概念图>`
- **明确禁忌**: `<用户明说不要的，例如：不要卡通化、不要灰渐变、不要紫光晕>`

## 5. 已决原则台账 (Decision Ledger)

> Critic 建议与本台账冲突时：**原则优先**，冲突记入本表并交人类裁决；禁止静默反转（防乒乓）。

| 编号 | 已决原则 / 决策 | 裁决人 | 日期 | 冲突记录 |
|:---:|---|---|---|---|
| P1 | `<例如：线胜于面——以线框/描边建立层级，不用背景色阶替代>` | 王辉 | `<日期>` | `<某轮 Critic 建议与之冲突的记录>` |
| P2 | `<例如：不使用紫/青霓虹光晕>` | 王辉 | `<日期>` | 无 |

## 6. 验收与仲裁
- **创意总监（用户）**: `<方向取舍、签收与终审归属，默认用户>`
- **门禁**: 三信号（结构清单全过 + 盲比有改进/已收敛 + 人类签收）——见 `references/critic-loop-protocol.md`
- **范围裁剪**: `<本方向不做什么，例如：不做暗黑模式>`

---

## 附加记录（D2/D3 追加）

### 评审账本 (Review Ledger)

> D2 累计派发硬上限 **5 轮**（作废轮次不计）；账本由主智能体落盘计数，超限自动熔断。

| 轮次 | 版本 nonce | 模式（盲比/排序） | 回执校验 | 结论（胜出维度 / 名次 / 无法区分） | 采纳与否决（理由） | 是否入账 |
|:---:|---|---|:---:|---|---|:---:|
| 1 | `<nonce>` | 盲比 | ✅ | `<A/B + 胜出维度>` | `<采纳 X，否决 Y（理由）>` | ✅ |
| 2 | `<nonce>` | 排序(4+1) | ✅ | `<名次与差距>` | — | ✅ |

- **累计派发**: `0/5`
- **分数遥测（可选，非门禁）**: `<若使用：记录分数 + 锚点版本 + 双 Critic 中位数；并标注"遥测口径">`

### 删除清单（D3 减法审计，每删一项记录一行）
| 元素 | 删除原因 |
|---|---|
| `<元素>` | `<为什么它不必要>` |
