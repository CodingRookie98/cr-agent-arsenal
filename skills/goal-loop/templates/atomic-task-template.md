# 原子子任务提示词规约 (Atomic Subtask Specification)

> **任务标识**: Task-[编号]: [任务简短名称]  
> **所属目标计划**: [关联实施方案计划路径]  
> **执行角色**: 后台执行智能体 (Execution Worker)  
> **当前状态**: [待执行 | 执行中 | 验证通过 | 阻断受阻]

---

## 1. 工作目录与目标文件清单
- **绝对工作目录**: `[项目根目录绝对路径]`
- **新增核心文件**: `[模块主文件相对路径，如：src/orders/calculator.ext]`
- **新增单测文件**: `[单测对应文件相对路径，如：tests/orders/test_calculator.ext]`
- **最小集成文件**: `[集成挂载文件相对路径，如：src/orders/index.ext]` (仅做接口挂载，严禁修改无关代码)

---

## 2. 数据契约与接口模型 (严格对齐，严禁猜测)
```[目标语言]
// 引用自项目核心公共契约: [相对路径]
[贴出确切的数据结构、接口字段与类型声明，保证单一真相源]
```

---

## 3. 依赖调用与 API 规范
- **允许调用的公共模块**:
  - `[模块路径/包名]`: 仅允许调用 `[指定函数/类/常量/枚举]`。
- **调用约束**:
  - 严格使用项目既有标准工具（如统一日志、统一异常类），严禁自造简易替代品；
  - 严禁调用未在文档或依赖清单中声明的私有 API。

---

## 4. 数据源与测试夹具规范
- **测试/基线数据源**:
  - 测试数据必须从 `[项目测试夹具统一路径]` 导入或按规范工厂函数构建；
  - **严禁**在业务逻辑或测试文件内硬编码随意的内联伪数据、未对齐的常量数组。

---

## 5. 架构规范与行为约束
- **错误处理**:
  - 遇到非法入参或异常状态时，必须显式抛出/返回项目约定的领域异常，严禁静默吞掉异常；
- **边界防御**:
  - 关键计算与状态流转需做非空与有效性校验，杜绝运行时未定义行为。

---

## 6. 自动化验收命令 (后台必须运行并通过全部检查)
```bash
# 1. L0 静态类型与语法检查
[项目的静态检查命令]

# 2. L1 单元测试执行 (先红后绿)
[针对本子任务的测试执行命令]
```

---

## 7. 严禁事项与安全红线 (Strict Guardrails)
1. **严禁越权编写任何计划、设计或总结文档**（如 `IMPLEMENTATION_PLAN.md`、`task.md`）！后台的唯一职责是编写目标生产代码与测试代码；
2. **严禁修改清单之外的任何文件**，严禁改动公共核心契约或未分配模块；
3. **严禁为了跑通测试而降低断言门槛**、删除原有测试用例或注释报错代码；
4. **严禁执行任何破坏性 Git 命令**（如 `git reset`、`git checkout .` 等）。
