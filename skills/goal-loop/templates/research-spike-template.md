# [调研主题/技术领域] 技术调研与开源选型备忘录 (Research Spike Note)

> **文档元数据**
> - **所属目标**: [关联的目标名称或实施计划路径]
> - **调研主题**: [明确需要调研的核心能力或库，如：分布式任务调度 / 高性能流式解析]
> - **调研日期**: YYYY-MM-DD
> - **调研智能体**: [research-subagent / 角色名称]
> - **触发条件**: [新引入第三方库 | 复杂通用功能实现 | 陌生技术领域探索 | 架构选型裁决]
> - **最终裁决**: [采用成熟开源方案 | 基于现有封装扩展 | 团队自研 (必须阐述硬性理由)]

---

## 1. 调研背景与核心诉求 (Background & Requirements)

### 1.1 功能与性能核心指标
- [指标 1: 例如吞吐量、延迟、并发模型]
- [指标 2: 例如平台兼容性、内存开销、包体积]
- [指标 3: 例如许可证要求 (如 MIT/Apache-2.0 友好，严禁 GPL)]

### 1.2 本地工程现状排查 (Local Codebase Audit)
> 防重复造轮子第一层：优先查验当前工程或依赖树中是否已有可复用实现。
- **排查命令**: 宿主代码库检索能力（如 `glob` / `grep`）
- **发现资产**: [工程内已有模块 A、公共工具库 B 或 "无相关现有资产"]
- **可复用性结论**: [无法完全满足当前需求，原因是...]

---

## 2. GitHub 社区开源生态横向评测 (GitHub Community Benchmarking)

> 通过 `gh` CLI 检索业界主流成熟开源实现并进行对比评估：
> 检索命令示例: `gh search repos "<keyword>" --sort=stars --limit=5`

| 候选项目 / 库名 | Star 数 / 活跃度 | 维护状态 (最新提交/Issue 响应) | 许可证 (License) | 核心优势 | 潜在缺陷 / 风险 |
|---|:---:|:---:|:---:|---|---|
| **方案 A** (如：`repo/candidate-a`) | 12.5k / 活跃 | 本周活跃 / 响应极快 | MIT | 性能极高、无多余依赖 | 学习曲线较陡，文档偏简 |
| **方案 B** (如：`repo/candidate-b`) | 5.2k / 稳定 | 1 个月内更新 | Apache-2.0 | 开箱即用、中文文档齐全 | 依赖较重，不支持某特定平台 |
| **方案 C** (如：`repo/candidate-c`) | 800 / 较弱 | 半年未更新 | GPL-3.0 | 功能契合度高 | 协议风险，维护停滞，排除 |

### 2.1 社区实战参考代码范式 (`gh search code`)
> 从高质开源代码库中提炼的最佳实践片段或架构模式：
```language
// 摘录自 candidate-a 中的核心设计模式或调用逻辑
```

---

## 3. 官方权威规范与真实 API 契约验证 (API & Spec Verification)

> 通过 `find-docs`、`context7-cli` 或 `web_search` 查证的第一手权威事实，杜绝版本滞后与幻觉：

- **官方文档源**: `https://...` 或 `find-docs <library>` 检索结果
- **目标版本**: `vX.Y.Z`
- **关键 API / 类型签名**:
  ```language
  // 官方真实接口定义
  ```
- **已知陷阱与破坏性改动 (Breaking Changes)**:
  - [例如：从 v2 升级到 v3 后某些同步方法被移除]
  - [例如：Node 18+ 原生支持某特性，无需外部 polyfill]

---

## 4. 架构选型裁决与自研论证 (Architecture Decision & Rationale)

### 4.1 最终选型方案
**选定方案**: `[采用候选项目 A / 引入轻量级开源封装 / 团队自研]`

### 4.2 自研理由充分性论证 (仅在决定自研时必填)
> ⛔ **系统红线**：若放弃上述已有高星成熟开源方案而决定“重新造轮子自研”，必须满足以下硬性条件之一并在此详尽论证：
- [ ] **性能/体积极限苛刻**：现有库体积过大（如引入 10MB 依赖仅用 1 个函数）或无法满足毫秒级/无 GC 性能需求；
- [ ] **许可协议阻断**：所有可用开源库均为传染性协议（GPL/AGPL），违反商业与工程合规；
- [ ] **架构契约严重冲突**：现有库强绑定特定框架/运行时，无法与现有轻量架构解耦；
- [ ] **自研成本远低于适配成本**：所需核心逻辑极薄（< 100 行自包含纯算法），引入依赖反而增加维护负担。
> **详细自研辩护理由**:
> [此处填写详实的架构论证与推演，作为阶段 1 计划制定的输入]

---

## 5. 阶段 1 实施建议 (Handover to Phase 1)
- **推荐安装命令**: `pnpm add <pkg>` / `cargo add <pkg>`
- **依赖隔离建议**: [是否需要建立适配器 Adapter / 门面 Facade 隔离外部依赖]
- **测试保护建议**: [测试策略裁定重点：针对外部依赖接口的集成测试 (L2)]
